python-dotenv
gspread
google-auth
google-auth-oauthlib   # OAuth de usuario (org bloquea llaves de service account)
openai            # cliente para DeepSeek (OpenAI-compatible)
anthropic         # solo si LLM_PROVIDER=claude
playwright
jinja2
cloudinary
python-telegram-bot
requests
pytz
tweepy           # crosspost a X/Twitter (OAuth 1.0)
curl_cffi        # ingesta IG (Fase 2): TLS de navegador real, evita el 429 anti-bot
opencv-python    # clasificación de fotos (Fase 3): caras Haar + nitidez Laplaciano
spotipy          # enriquecimiento Spotify (Fase 4): popularity, géneros, releases
# OCR de flyers (Fase 3): binario del sistema, `brew install tesseract tesseract-lang`
fastapi          # GUI local de curación (web/)
uvicorn          # servidor de la GUI: uvicorn web.app:app
python-multipart # forms HTML en FastAPI (edición inline de la GUI)
rapidocr_onnxruntime  # OCR potente para flyers (Fase 5): lee tipografía artística
cryptography     # secretos por marca cifrados en DB (Fernet) — portal
httpx            # cliente HTTP de la API (Resend) y TestClient
email-validator  # EmailStr en la API
